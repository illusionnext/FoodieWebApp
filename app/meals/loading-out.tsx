import classes from "./loading.module.css";

export default function MealsLoadingPage() {
  return <h1 className={classes.loading}>Meals are loading...</h1>;
}
//it works with the loading component any page next to it or any nested page
